# たびのしおり 公開ロードマップ

ゴール: App Storeでの一般公開。
方針: **無料でできる改善をすべて先に行い、納得してから課金ステップ
(Apple Developer 年99ドル)に進む。**
各フェーズの最後に「チェックポイント」があり、そこで次に進むか判断する。

収益の位置づけ: 維持費(Apple Developer 年99ドルのみ)を回収できたら成功。
サーバーを持たないので、ほかに固定費はない。

## プロダクト方針

- ポジショニング: 「同行者と一緒に作って、旅行中に片手で見る旅のしおり」
- ターゲット: 複数人で旅行に行く人(家族・友人・カップル)
- **差別化の軸は「旅行中に強い」**(競合はどれも計画ツールで出発後が薄い)。
  根拠と競合の実データ: [docs/competitive-landscape.md](docs/competitive-landscape.md)
- 獲得は**日本語圏**。インバウンド特化はしない。ただし**共有相手の言語で表示できる**
  道は残す(A-1 で i18n の下地だけ入れる)
- 収益モデル: **自動更新サブスクのみ / 月額¥300・年額¥1,800 / 広告なし**。
  **無料は個人利用のすべて。Pro が要るのは「共有を始めるとき」だけで、
  受け取る側は永久に無料**(→ [docs/pricing.md](docs/pricing.md))
- 共有は**サーバーを持たない**。しおりを1ファイルにして送り合う非同期方式。
  結果として **プライバシーラベルは「データを収集していません」**
  ── 競合が絶対に言えない訴求。これを崩す変更は慎重に判断する
- 通信: **なし**
- **UI/UX の詳細設計は [docs/ux-design.md](docs/ux-design.md) が正**
- **やらないこと**(要望が出ても足さない。理由は docs/ux-design.md §11):
  - **広告**(「データを収集していません」を失う。検討済み・不採用)
  - アカウント登録 / Sign in with Apple(5.1.1(v) のアカウント削除要件を回避)
  - インバウンド特化・多言語での獲得(competitive-landscape.md §4)
  - 乗換検索(NAVITIME に勝てない)/ AI旅程生成 / 予約・決済
  - **取り込み(受け取る側)の有料化 / 送り返しの有料化**(共有が死ぬ)
  - 無料プランでの旅の数の制限 / 解約時のデータロック(docs/pricing.md §10)
  - 週カレンダー表示 / 経路探索API

## フェーズA: アプリ品質の底上げ(費用: 0円)

Web/PWAのまま、毎日使って気持ちいいレベルまで磨く。**広告はまだ入れない**
(先に中身を作る。広告枠の場所はレイアウト設計時に確保だけしておく)。

- [x] A-0 **あとから変えると高くつく土台**(先に置く)
  - [x] i18n の下地(文言を `src/i18n/` に集約、日付・通貨は `Intl`)
  - [x] Dexie スキーマ(**並び順は fractional indexing**、同期に備えた
        `updatedAt` / `deletedAt` / `deviceId` を最初から持たせる)
  - [x] カテゴリ自動推定の辞書 / 外部地図URLの生成
- [x] A-1 コア機能(旅一覧 / Day タブ / タイムライン / 名前だけの追加 /
      詳細シート / カテゴリ自動推定 / 外部地図リンク / 複数リンク / 設定)
      — 実際にブラウザで動かして確認済み。現在時刻ラインと起動時の着地点も入れた
- [x] A-2 旅程を「回せる」仕掛け
  - [x] 移動コネクタ(**移動時間は手入力**。座標を得る手段が無く、場所検索APIは
        クエリが外部に出るので使わない → docs/ux-design.md §3.3)
  - [x] 空き時間表示(2時間以上)/ **間に合わない警告**
  - [x] コネクタから経路を地図アプリで開く(**座標が無くても場所名で出せる**)
  - [x] リフロー(±15/30/60分)+「元に戻す」/ 📌 固定は動かさない
  - [x] 完了チェック(右スワイプ)/ 削除(左スワイプ)
  - [x] 長押しのアクションメニュー(上下移動 / 別の日へ / 複製 / 固定 / 削除)
  - **ドラッグ&ドロップは不採用**(横スワイプとジェスチャが衝突する。
    時刻ありは時刻順に並ぶので上下ボタンで足りる → docs/ux-design.md §4.5)
- [x] A-3 最初の一歩を軽くする
  - [x] 起動時の着地点(進行中の旅の今日へ直行)— A-1 で実装済み
  - [x] **ようこそ1枚**(初回のみ)。何のアプリかと**何を要求されないか**だけ
  - [x] **空状態のチップ**(タップで即追加。Day 1 / 中日 / 最終日で内容を変える)
  - [x] **長押しヒント**を文脈で1回だけ(時刻ありが2件以上の日にだけ出る)
  - **上前の3枚カルーセルは不採用**(読まれない。非自明な価値はその場面で教える
    → docs/ux-design.md §2.1)
- [x] A-4 パフォーマンス — **測ってから決めた**
  - 本番ビルドでタイムラインが見えるまで **89ms**。バンドルは 345KB(gzip 111)で、
    その **86% が React と Dexie**(どちらも起動時に必要)。自前コードは 14%
  - **React.lazy での分割はしない。** 分けられるのは自前の 49KB の一部だけで、
    効果は数%。分ける対象の「重いライブラリ」がそもそも無い
  - [x] **本当の問題を直した**: スプラッシュが `launchShowDuration: 3000` の
    固定待ちで、98ms で準備できているのに3秒見せていた。
    `SplashScreen.hide()` を描画完了時に呼び、保険の固定待ちも1.5秒へ
  - [x] **バンドルの上限**(`npm run size`)を CI に追加。
    今のサイズより「あとから重いものが入る」ほうがリスクなので、そこに門を置いた
  - [x] ドライバに `perf` モードを追加(本番ビルドを配って起動を実測する)
- [x] A-5 「紙のしおり」のビジュアル確立 / ダークモード
  - [x] **Day タブを冊子のインデックスタブに**(選択中は下の角を落として本文と地続き)
  - [x] **器のアイコンを SVG に**(🗺 は 16px では判読不能だった)。
        カテゴリの絵文字はそのまま(色6系統 + 形の設計)
  - [x] タイムラインの節を円に(レール上の点として読ませる)
  - [x] 済んだ予定: 取り消し線は名前だけ、節と時刻も落とす
  - [x] 未設定のコネクタを静かに(同じ行が並ぶと旅程より目立ってしまう)
  - [x] **大きい文字(22px相当)で崩れないことを確認**
  - **広告枠は空けない**(広告不採用が決まったため)

**チェックポイントA**: 自分・家族で1〜2週間使い、毎日の利用が苦にならないか確認。

## フェーズB: 公開に必要な土台(費用: 0円)

- [x] B-1 Capacitorプロジェクト化(com.tsukune.travelnote、共有スキーム、
      ITSAppUsesNonExemptEncryption=false)
- [x] B-2 法務文書を実内容で記述。運営者 Shintaro Fujisawa /
      連絡先 tsukune.devlab@gmail.com。**アプリの実装そのものを書いた**ので、
      実装を変えたら必ずここへ戻る。設定・購入画面・ようこそ画面からリンク済み
- [x] B-3 GitHub Pages の中身(紹介ページ + 法務2枚 + 共通CSS)。
      CI と同じ手順で組んでブラウザで確認済み(リンク切れ・404 なし)
- [x] B-3b **GitHub リポジトリ `tsukune131/TravelNote` を Public で作成して push**
      ── ここだけユーザー作業。push すると Actions の
      「Deploy to GitHub Pages」が走り、
      https://tsukune131.github.io/TravelNote/ が公開される。
      アプリ内のリンク先もこの URL(`src/lib/openExternal.ts` の `LEGAL_BASE`)。
      **App Store Connect が到達できるプライバシーポリシーURLとして必要**
- [x] B-4 **非同期共有(サーバーなし)**(→ docs/ux-design.md §6)
  - [x] Snapshot 形式(書き出し・読み込み)と baseline
  - [x] 3方向マージ(ぶつかった Day だけ「案」に分ける)— 実行して検証済み
  - [x] 書き出し/取り込みの画面と共有シート連携(実機の受け取りは C-5 で確認)
  - [x] 案の切替と「この案を採用」の画面
  - [x] 「未送信の変更 N件」バッジ、取り込み後の変更一覧
  - [x] 表示名の設定(既定は「ゲスト」。アカウント登録はしない)
  - [x] **旅一覧にも取り込み口**(受け取る人は旅を1つも持っていない)

> **リアルタイム同期は保留。** 実際に旅行で使って「送り返すのが面倒」という
> 不満が本当に出たら、そのとき Firestore 等で復活させる(チェックポイントCで判断)。
> データモデルは両方に対応できるので後戻りのコストはほぼない。
- [x] B-5 **収益モデルの決定** → サブスクのみ / 月¥300・年¥1,800 / 広告なし。
      **共有を始めるときだけ Pro、受け取る側は無料**
      (→ [docs/pricing.md](docs/pricing.md))
- [x] B-6 **無料 / Pro の線引きを画面に落とす** — 済み(2026-08-10)
  - [x] 判定ロジック(`src/pro/entitlement.ts`)— 実行して検証済み
  - [x] 「しおりを送る」を押したときのペイウォール(ここが唯一の課金点)
  - [x] 購入画面の見た目(3.1.2 の必須表示)。**課金の配線は D-0**、価格も StoreKit から取る
  - [x] 解約後も既存の旅が編集でき、共有済みの旅を送り続けられることの確認
        — 済み(2026-08-10)。判定は純粋関数なので**実行して14項目を確認**した
        (編集は無条件で可・共有済みは1年間送れる・窓を過ぎると window-expired・
        取り込んだ旅も同じ窓で切れる)。実機で見るのは StoreKit が失効を返すかだけ

**チェックポイントB**: ここが課金判断ポイント —
納得したら Apple Developer 登録(年99ドル)へ。
**サーバーを持たないので、固定費は年99ドルのみ。**

## フェーズC: TestFlight配布(費用: 年99ドル)

- [x] C-1 Apple Developer Program 登録 — 済み(2026-08-09 時点)
- [x] C-2 TestFlight自動配布パイプライン(手順: docs/ios-release-setup.md §0)
  - [x] ワークフロー / fastlane 3レーン / 共有スキーム / 相対パスビルド
  - [x] **証明書リポジトリは `VitaNote-certificates` を共用**する決定。
        Apple の配布用証明書は2枚まで。別リポジトリにすると match が
        2枚目を作り、期限切れ時の乗り換え枠が無くなる(→ docs §0-0)。
        `TravelNote-certificates` は不要になったので削除する
  - [x] **Apple 側の設定**(App ID / アプリ登録 6799566207)。API キーと Team ID は
        **VitaNote のものを流用**(チーム単位のため)
  - [x] **PAT を作り直す**(旧 PAT は 2026-08-09 に露出させたため失効)。
        対象は `VitaNote-certificates`・Contents 読み書き
  - [x] **GitHub Secrets 7つ**を登録
  - [x] lane=certificates → lane=beta。**TestFlight へのアップロード成功**
        (配布用証明書は1枚のまま。共用リポジトリにプロファイルが1つ増えただけ)
- [x] C-3 ネイティブ機能 — **閉じた**(2026-08-09)。足すものが無くなった
      - スプラッシュは A-4 で実装済み、実機でも確認済み
      - **予定リマインド通知は不採用**(→ docs/ux-design.md §11)。
        しおりは開いて見るもので、鳴って割り込むものではない。
        **通知権限のダイアログを出す必要も消えた**
      - **触覚も入れない**(2026-08-09)。手触りは上がるが、そのために
        ネイティブ依存を1つ増やす。**Capacitor プラグインは5つのままで足りている**
        (app / browser / filesystem / share / splash-screen)。
        依存が少ないほど iOS の更新に追従しやすく、審査で説明することも減る
- [x] C-4 **Share Extension**(Safari の共有シートから食べログ等を取り込む)
      ── **完了(2026-08-10)。実機で共有 → 帯 → 配置まで通った**
  - [x] 拡張ターゲット(`ios/App/ShareExtension/`)。結果を一言出して 0.7 秒で閉じる
  - [x] `project.pbxproj` を手で編集(Xcode を持たないため)。
        **`xcode` パッケージでパースして検証済み** ── 2ターゲット・未定義参照なし・
        埋め込みフェーズ `dstSubfolderSpec=13`・Bundle ID と entitlements の対応
  - [x] App Group `group.com.tsukune.travelnote` の entitlements を両ターゲットに
  - [x] アプリ側のインボックス(Dexie v2 + 起動/前面復帰で取り込み + 配置画面)
        — **ブラウザで検証済み**(届く → 旅を選ぶ → リンク付きの予定になる)
  - [x] **実機で共有シートに出るところまで確認**(2026-08-10)。
        pbxproj・拡張のコンパイル・埋め込み・2ターゲットの署名は通った
  - [x] **受け渡しを作り直した**(2026-08-10)。preferences の group は
        App Group ではなくキーの接頭辞だった → App Group のファイル →
        SceneDelegate → Documents → Filesystem に変更。preferences は依存から外した
  - [x] **入口を旅一覧へ移した**(2026-08-10)。旅の中に置くと旅の数だけ同じ帯が出る
  - [x] fastlane を2ターゲットぶんの署名に(match / update_code_signing_settings /
        export_options をそれぞれ別々に指定)
  - [x] **ユーザー作業(Apple Developer ポータル)** — 手順は docs/ios-release-setup.md §7.6。
        App Group を作る → 拡張用 App ID `com.tsukune.travelnote.share` を作る →
        両方の App ID で App Group を **Configure** → **`lane=refresh_profiles`**
        (**`certificates` では足りない**。force が無く本体の既存プロファイルを
        作り直さないので、App Group が入らないまま落ちる)
  - [x] **実機で確認(作り直したぶん)**: 共有 → 帯 → 配置まで通った(2026-08-10)
- **地図ビューは不採用**(2026-08-09 → docs/ux-design.md §11)。
      外部の地図アプリに投げるだけで足りる。地図SDKは「データを収集していません」を
      失いかねず、1画面のために最大の資産を賭けない
- [x] C-5 **`.tabishiori` の受け取りを実機で確認** — 済み(2026-08-09)
  - [x] 書類タイプ(UTI `com.tsukune.travelnote.shiori`)を Info.plist に宣言
  - [x] `appUrlOpen` の受け口を App に配線(**書いたまま呼んでいなかった**)
  - [x] **実機で確認**: 送った .tabishiori をタップしてアプリが開き、取り込まれた。
        **サーバーを持たない共有が、実機で端から端まで成立した**
  - [x] 実機で1件みつけて修正: **消した旅を取り込み直しても復活しなかった**
        (墓標が勝っていた)。あわせて order の衝突も直した
  - [x] **2026-08-10 に再発 → 再修正。** 「開いてもアプリが起動するだけで
        取り込みが始まらない」。原因は受け取ったファイルの読み方で、
        `directory` 無しのとき Capacitor は**完全な `file://` URI** を期待するのに、
        `file://` を剥がした素のパスを渡していた。`invalidPath` を `catch {}` で
        握りつぶしていたので無反応に見えた。**渡し方を順に試すようにし、
        読めなければ画面に出す**(黙って終わらせない)。
        ⚠️ 08-09 には通っていたので、間に何かが変わっている
        (有力なのは C-4 で足した `SceneDelegate`)。**確定はしていない。**
        同じ症状が出たらまずここを疑う
- [x] C-6 実機確認(前面復帰・機内モードでの編集)— 済み(2026-08-09)
  - [x] **機内モードで旅程の編集がふつうに動く。** 差別化の軸そのものの裏付け
        (tabiori はオフラインでは閲覧しかできない → competitive-landscape.md)
  - [x] 地図への遷移だけは当然つながらない。**これは直さない** ──
        地図は外部アプリに投げる設計なので、オフラインでの見え方は
        相手のアプリの都合。こちらで通信状態を監視して警告を出すのは、
        依存を1つ増やしたうえで**ユーザーが既に知っていることを言うだけ**
  - 権限ダイアログの確認は不要になった(通知を不採用にしたため、
    このアプリは**権限を1つも要求しない**)
- [x] C-7 TestFlightで配布して実際に使った — **2026-08-10**

**チェックポイントC — 通過(2026-08-10)。フェーズDへ進む。**

実際に使ったうえで「完成している」と判断。したがって:

- **リアルタイム同期は復活させない。**「送り返すのが面倒」という不満は出なかった。
  ファイルを送り合う非同期方式のままでいく ── これが
  「データを収集していません」を支えている(→ プロダクト方針)
- **Share Extension は入れた**(C-4)。ただし作り直したぶんの実機確認が残っている。
  フェーズDと並行で一度 `beta` を流して見ておく
- 残る積み残しは B-6 の「解約後も編集できる」確認だけで、これは D-0 の
  課金を配線したあとでしか確かめられない

## フェーズD: 収益化と App Store公開(追加費用: 0円)

- [ ] D-0 **サブスクの実装**(→ [docs/pricing.md](docs/pricing.md))
  - [x] **有料App契約(Paid Applications Agreement)+ 税務・銀行情報**
        ── 締結済み(2026-08-10 確認)。**アカウント単位**の契約なので、
        別アプリで済ませてあればそのまま使える。待ち時間はもう無い
  - [x] **App Store Small Business Program に登録**(手数料 30% → 15%)
        ── 登録済み(2026-08-10 確認)。これも**アカウント単位**なので、
        別アプリで申請してあれば効いている
  - [x] サブスクリプショングループ `travelnote_pro` と製品を作成
        (`...pro.monthly` ¥300 / `...pro.yearly` ¥1,800)— 済み(2026-08-10)
  - [x] **無料トライアルは付けない**(2026-08-10 決定)。
        課金点が「はじめて共有するとき」の1か所しかないので、トライアルは
        実質「14日間ためしに共有できる」になる。旅行の計画は2週間で
        終わらないことが多く、**価値を感じきる前に切れる**。加えて、一度送れば
        その旅は**解約後も送り続けられる**ので、トライアルだけで目的を
        達してしまう人が出る。**伸び悩んだら後から足す**(追加はできるが、
        外すのは既存契約者に影響が出るので、始めは付けないほうが安全)
  - [x] **審査担当者が共有を試せる導線** — 済み(2026-08-10)。
        **1台の端末だけで往復できる**ことに気づいたので、それを審査メモに書いた:
        送る → iOS の共有シートで「"ファイル"に保存」→ ファイルアプリでタップ →
        共有 → たびのしおり → 取り込まれる。相手も2台目も要らない。
        あわせて `store/review-sample-kyoto.json`(添付用のサンプル)を生成。
        **アプリ本体の `parseSnapshot` を通してから書き出している**ので形式のズレが無い。
        日本語UIなので英語版のメモも併記した
  - [x] `@capgo/native-purchases` 導入(StoreKit 2 直叩き。**静的import**。
        RevenueCat は使わない)— 済み(2026-08-10)
  - [x] 購入画面: **名称・期間・価格・規約とポリシーへのリンク・購入を復元**(3.1.2)
        — 済み(2026-08-10)
  - [x] **価格が円で出ない問題**(2026-08-10)。Sandbox のストアフロントが
        米国に落ち、ドル表示から抜けられなかった(サンドボックステスターを
        日本にしても不可)。**円で返ったときだけ StoreKit を信じ、
        円以外は日本の定価を出す**(`src/ui/Paywall.tsx` の `priceOf`)。
        日本のみ配信なので本番では常に StoreKit の値が出る
  - [x] Sandbox で**購入・復元**を実機確認(2026-08-10)。
        削除 →(機内モードで起動して無料に見えること)→ 復元 で Pro に戻る、
        まで通した。**StoreKit 2 は同じ Apple ID なら復元を押さなくても
        `currentEntitlements` で返る**ので、復元ボタン単体を試すには
        この手順(機内モードを挟む)が要る
  - [ ] **解約の実機確認。** 判定側は実行して確認済みなので、
        ここで見るのは **StoreKit が失効を返すか**だけ
        (`getPurchases({onlyCurrentEntitlements:true})` が空になるか)。
        Sandbox は更新周期が圧縮されているので、解約して待てば期限切れになる。
        そのあと**未共有の旅で購入画面が出る / 共有済みの旅はそのまま送れる**を見る
  - [x] 未購入の端末で購入シートのスクリーンショットを撮った(2026-08-10・`photo/IMG_2480.PNG`)。
        **¥300 / ¥1,800 で写っている。** 3.1.2 の必須要素(名称・期間・価格・
        規約・ポリシー・購入を復元・自動更新の説明)がすべて1枚に収まっている
        - ⚠️ この1枚は**リード文にアスタリスクが出ていた版**。審査上は問題ないが、
          次に未購入状態を作る機会があれば撮り直す
- [x] D-1 ストア素材 — **済み**(2026-08-10)。store/appstore-listing.md
      - [x] サブタイトル(22字)・プロモ・説明文(941字)・キーワード(86字)。
            **キーワードは名前・サブタイトルと1語も重複させていない**
      - [x] スクリーンショット5枚(1320x2868)を `store/screenshots/` に組み上げ。
            2枚目は共有シートと届いたリンクを**重ねて合成**した
            (縦長の端末を横に並べても縦長キャンバスは埋まらないため)
- [ ] D-2 **プライバシーラベル申告 =「データを収集していません」**
      (サーバーなし・広告なし・解析なし。決済は Apple が扱う。
      根拠は store/appstore-listing.md に記録する)
- [ ] D-3 提出前チェック(release-auditor エージェント)→ 審査提出
      (リジェクト往復1〜2回を想定)
- [ ] D-4 公開後の運用(クラッシュはApp Store Connectのレポートで監視、
      **サブスクの継続率と、月額/年額の比率**を確認、iOSアップデート追従)

## 費用まとめ

| フェーズ | 費用 |
|---|---|
| A・B | 0円 |
| C以降 | Apple Developer 年99ドル のみ |

## 現在地

- 2026-08-08 プロジェクト立ち上げ(/new-ios-app)。
  表示名「たびのしおり」/ Bundle ID `com.tsukune.travelnote` /
  リポジトリ `tsukune131/TravelNote` を確定。
  収益は「無料+広告」、通信ありの方針を採用したため、
  雛形の「完全ローカル・広告なし」前提を本アプリ向けに書き換えた。
- 2026-08-08 UI/UX を設計(docs/ux-design.md)。
  当初案の「週カレンダー」を **Day タブ**に変更、時刻を任意化、
  **移動コネクタ**と**リフロー**を核の機能として追加。
  **同行者との共有・同時編集**を要件に採用したため、
  完全ローカルを外して同期基盤を B-4 に新設。
  アカウントは作らせない方針(5.1.1(v) 回避)。
- 2026-08-08 競合調査(docs/competitive-landscape.md)。
  **tabiori(★4.5 / 6万件 / 現役)が直接の競合**と判明。同じ土俵で戦わず、
  差別化を「**旅行中に強い**」に確定。tabiori の実際の不満(時刻入力が必須・
  オフラインは閲覧のみ)に、本アプリの既存設計がそのまま答えになっている。
  **インバウンド特化は不採用**(ドッグフーディングを失う・海外での発見導線がない・
  英語だけでは足りない)。代わりに共有相手の言語で表示できる道だけ残す。
  **収益モデルは未確定に差し戻し**(tabiori のサブスク成立を見て再検討)。
- 2026-08-08 A-0(あとから変えると高くつく土台)を実装。
  i18n / fractionalIndex / plainDate / category / maps / Dexie スキーマ / repo。
- 2026-08-08 **共有をサーバーなしの非同期方式に決定。**
  しおりを1ファイルにして送り合い、baseline との3方向マージで取り込む。
  **両方が同じ日を直したときは、どちらも捨てず「案」として両方残す**(粒度は Day)。
  これにより**プライバシーラベルを「データを収集していません」に戻せた** ──
  競合が言えない訴求で、維持費も永久に0円。
  リアルタイム同期は捨てずに保留(チェックポイントCで再判断)。
  `src/share/`(snapshot / merge / apply)を実装し、9ケースを実行して検証。
- 2026-08-08 **収益モデルを確定: 自動更新サブスクのみ / 広告なし。**
  広告を捨てたことで「データを収集していません」が完全に一貫した
  (サーバーなし・広告なし・解析なし・決済は Apple)。画面の広告枠確保も取り消し。
- 2026-08-08 **価格と線引きを確定: 月¥300 / 年¥1,800。**
  **無料は個人利用のすべて**(旅の数も機能も制限しない)。
  **Pro が要るのは「まだ共有していない自分の旅を、はじめて送るとき」だけ。**
  取り込み・送り返し・共有済みの旅の再送は**永久に無料** ──
  受け取る側に課金を求めると共有そのものが死ぬため。
  tabiori(¥480/¥3,900)より月37%・年54%安い。サーバーがないから下回れる。
  `src/pro/entitlement.ts` に純粋関数として実装し、実行して検証。
  **無料トライアルの有無は D-0 で最終決定。**
- 2026-08-08 **A-1 完了。** 旅一覧・Dayタブ・タイムライン・名前だけの追加・
  詳細シート・カテゴリ自動推定・外部地図リンク・複数リンク・設定を実装。
  現在時刻ラインと「起動したら今日にいる」も入れた。
  実機の代わりに Chrome を 390×844 で駆動して全フローを通し、
  コンソールエラーなし・ライト/ダーク両方を目視確認。
  カテゴリ推定の的中は実データ7件中4件(店名は当たりにくい。1タップで直せる前提)。
- 2026-08-08 `.claude/skills/run-travelnote/` を追加。playwright-core から
  既存の Chrome を叩いて、実機の代わりに iPhone 相当で駆動する。
  smoke(通し)と repl(対話)の2モード。
- 2026-08-08 **A-2 完了。** 移動コネクタ・空き時間・間に合わない警告・
  リフロー(±15/30/60分)と元に戻す・完了チェック(右スワイプ)・削除(左スワイプ)・
  長押しのアクションメニュー(上下移動 / 別の日へ / 複製 / 固定)。
  **移動時間は手入力**にした(座標を得る手段が無く、場所検索APIを叩くと
  クエリが外部に出て「データを収集していません」が崩れるため)。
  **ドラッグ&ドロップは不採用**(横スワイプと衝突。上下ボタンで代替)。
  純粋ロジック(connector)を実行検証し、ブラウザでも全操作を通した。
- 2026-08-09 **A-3 完了。** ようこそ1枚・空状態のチップ・文脈で出す長押しヒント。
  **上前の3枚カルーセルは不採用**にした(読まれないし、このアプリの非自明な価値は
  その場面が来たときに教えるほうが効く)。初回にまとめて設定を聞くのもやめ、
  地図アプリは初回タップ時、表示名は共有参加時に聞く。
- 2026-08-09 **A-4 完了。測ってから決めた。**
  本番ビルドで**タイムラインが見えるまで 89ms**、バンドル 345KB(gzip 111)。
  内訳は React 53% / Dexie 30% / Capacitor 3% / **自前コード 14%** なので、
  **React.lazy での分割はしない**(効果が数%しかない)。
  代わりに本当の問題を直した ── **スプラッシュが3秒の固定待ちで、
  98msで準備できているのに待たせていた**。描画完了で閉じるようにし、
  保険の固定待ちも1.5秒へ。あわせて `npm run size` の上限を CI に置いた
  (今のサイズより、あとから重いものが入るほうがリスクなので)。
- 2026-08-09 **A-5 完了。フェーズA の作業はすべて終わり。**
  Day タブを冊子のインデックスタブに、器のアイコンを SVG に(🗺 が潰れていた)、
  タイムラインの節を円に、済んだ予定の取り消し線を名前だけに。
  スクロール時に Day タブの下線が途中で切れるバグも修正
  (線をスクロールする箱の中に置いていた)。
  大きい文字(22px相当)でも崩れないことを確認。
- 2026-08-09 **実際に使ってのフィードバックを反映**(チェックポイントAの第1回)。
  - **並びを作り直した。** 「時刻未定を末尾へ落とす」のは、
    「時刻は決めなくていい」と言いながら未入力を格下扱いしていた設計ミス。
    並びは入力順(order)一本にし、**時刻を入れた瞬間に一度だけ**位置を直す方式へ
  - **時刻入力の入口を2つ足した。** 行の時刻欄を直接タップ / 追加バーで
    「9:00 二条城」と書く。詳細シートを開かせない
  - **アイコンをタップしてカテゴリ変更**(シートの奥から浅いところへ)
  - **並べ替えのつまみ(三本線)を追加。** 専用のつまみからしかドラッグが
    始まらないので、横スワイプと衝突しない ── 見送っていた理由が解けた
  - 文言を「同行者」から「友達や恋人」へ
- 2026-08-09 **見た目の方向を転換**(チェックポイントAの第2回)。
  「ださい / 女子旅で開いてテンションが上がる感じに」というフィードバック。
  最初の 生成り + 藍 + 明朝 は**和・落ち着き・大人**で、狙いと逆を向いていた。
  紙の比喩は残したまま **「きちんとした和の冊子」→「デコった手帳」** へ:
  桜色の地・コーラルの差し色・夕焼けのグラデーション・丸ゴシック・
  キャンディ色のカテゴリ・葡萄色を含んだ暗い配色。
  **予定を白いカードに載せた**のが構造上いちばん効いた
  (「全部ピンク」ではなく「ピンクの紙に貼った付箋」に見える)。
  グラデーションは主ボタンと選択中の Day タブだけに留め、面では使わない。
  → docs/ux-design.md §8.0
  **※ 丸ゴシックは iOS にしかない。実機で見え方を確認すること。**
- 2026-08-09 **フェーズB に着手。B-1 / B-4 / B-6 の画面が完了。**
  しおりの受け渡しを配線した(書き出し → 共有シート / 取り込み → マージ)。
  **A→B→A の往復を、別コンテキスト(= 別端末・別 IndexedDB)の
  ブラウザ2つで実際に通した。** 衝突 → 案に分かれる → 見比べる → 採用、まで。
  **旅一覧にも取り込み口を足した** ── 受け取る人は旅を1つも持っていないので、
  そこに口が無いと何もできなかった(往復を試して詰まった)。
  ペイウォールは「自分の旅をはじめて送るとき」だけ出る。
  **課金の配線は D-0** なので、いまは「このまま送る」で抜けられる。
- 2026-08-09 **B-2 / B-3 完了。フェーズB の作業はすべて終わり。**
  運営者 Shintaro Fujisawa / 連絡先 tsukune.devlab@gmail.com で
  プライバシーポリシーと利用規約を実内容で記述。
  **アプリの実装そのものを書いた**(サーバーなし・アカウントなし・広告なし・
  解析なし・決済は Apple・共有はユーザー自身がファイルを送る)ので、
  **実装を変えたら必ずここへ戻る**。
  利用規約には 3.1.2 が要求するサブスク条項(期間・価格・自動更新・解約方法・
  解約後も既存の旅は編集でき共有済みの旅は送り続けられること)を明記。
  紹介ページも作り、CI と同じ手順で組んでブラウザで確認(リンク切れ・404 なし)。
- 2026-08-09 **フェーズB 完了。公開まで到達。**
  `tsukune131/TravelNote` を Public で作成して push。
  **https://tsukune131.github.io/TravelNote/ が生きている**
  (紹介ページ・プライバシーポリシー・利用規約 すべて 200)。
  ── App Store Connect が要求するプライバシーポリシーURLが用意できた。
  Pages の有効化は GITHUB_TOKEN では作成できず、一度だけ API で有効化した
  (`gh api -X POST repos/.../pages -f build_type=workflow`)。
  初回の CI は **@capacitor/browser の宣言漏れ**で落ちた ── 手元の node_modules に
  残っていて、ローカルだけ通っていた。`npm run deps` の門を足して塞いだ。
  **→ チェックポイントB通過。** Apple Developer は登録済みとのことで、フェーズCへ。
- 2026-08-09 **フェーズC 着手。** `docs/` を .gitignore に入れた
  (競合分析と価格戦略が Public で見えていたため。**手元には残る**。
  ⚠️ 履歴にはまだ残っている)。
  C-5 の書類タイプ(UTI)を宣言し、`appUrlOpen` の受け口を App に配線
  ── **書いたまま呼んでいなかった**ので、受け取っても何も起きない状態だった。
  証明書リポジトリを Private で作成。
  **残るは Apple 側の設定と Secrets の登録(ユーザー作業。docs/ios-release-setup.md §0)。**
- 2026-08-09 **C-2 完了。TestFlight へのアップロードに成功**(App 6799566207)。
  途中 `Repository not found` で2回落ちた。原因は **MATCH_GIT_URL が
  古い `TravelNote-certificates` を指していたこと** ── 共用への変更で
  手順書の §0 だけ直し、テンプレート由来の §4〜§6 を直し忘れていた。
  Private リポジトリの 404 は「無い」ではなく「そのトークンから見えない」の意味。
  切り分け用に `scripts/check-match-auth.sh` / `.ps1` を追加した。
  **証明書は1枚のまま**で、共用リポジトリに TravelNote のプロファイルが増えただけ。
- 2026-08-09 **「人ごとに違う値を持つ」を作って、外した。**
  集合場所までの移動時間を人ごとに持ち「何時に出るか」を逆算する機能を
  実装(同行者の登録・人ごとのマージ例外まで)したうえで、**不採用**
  (docs/ux-design.md §11)。人ごとの値を1つ入れると、そこから先がすべて増える。
  **しおりは「みんなで見る1枚の紙」であって、各自の予定表ではない。**
  複数人への対応は、共有(1ファイルを送り合う)とマージだけで足りる。
  同じ判断で、チームに分かれて後で合流する案も不採用。
  ── 参加者一覧の並びが不定だった不具合の修正だけ残した。
- 2026-08-09 **旅に写真アルバムのリンクを持たせた**(docs/ux-design.md §5.3)。
  旅の写真は LINE アルバムや Googleフォトにあって、しおりの中にはない。
  旅が終わったあとにしおりを開いても、いちばん見たいものへ行けなかった。
  **旅の一覧のカードから1タップ**で開く。写真は取り込まないので、
  容量もプライバシー申告も動かない。あわせて、アプリが持っているリンク
  (LINE / Googleフォト / iCloud写真)は**アプリ内ブラウザではなく外に出す** ──
  Universal Link が効かず web 版が出るだけで写真に届かないため。
- 2026-08-09 **TestFlight の実機で確認できたもの**(Windows では確かめようがなかった分):
  アプリアイコンと起動画面(配色変更前の生成り色のちらつきが消えた)/
  並べ替えが指に追従して離すと落ち着く / 日付欄の枠が重ならない・
  「＋ 時刻」のチップが出る / 移動時間シートの下に追加バーが残らない /
  Googleフォトの共有リンクからアプリが開く(`isAppLink` の判断が正しかった。
  地図を開く経路も同じ `openOutside` なので、2回目以降も開く裏付けになる)。
  **残るのは C-5(`.tabishiori` の受け取り)だけ。**
- 2026-08-09 **「作ってあるのに、たどり着けない」3件を繋いだ**:
  旅の削除(`deleteTrip` は実装済みでボタンが無かった)/ 予約情報の入力
  (🎫バッジの描画はあったが設定するUIが無く、**絶対に出ないバッジ**だった)/
  参加者の一覧(あわせて `ensureOwner` の呼び忘れを修正 ──
  **送っても参加者0人のしおりが飛んでいた**)。
  続けて**準備画面**(持ち物・予約まとめ・旅のメモ)を実装し、
  ⋯ を設計どおりのメニューにした(docs/ux-design.md §7.1)。
- 2026-08-09 **C-5 完了。サーバーを持たない共有が実機で端から端まで成立した。**
  送った `.tabishiori` をタップ → アプリが開く → 取り込まれる、まで通った。
  途中で1件みつけて直した:**消した旅を取り込み直しても復活しない**
  (論理削除の墓標が突き合わせに勝ち、旅の画面は開けるのに一覧にだけ出ない)。
  取り込みは「これを手元に持つ」という明示の意思表示なので、墓標より優先する。
  **これでフェーズCで残るのは実機の総点検(C-6)と、実際に旅行して使うこと(C-7)だけ。**
- 2026-08-09 **C-6 完了。機内モードで旅程の編集がふつうに動いた。**
  差別化の軸「旅行中に強い」の裏付けそのもの ── 競合(tabiori)は
  オフラインでは閲覧しかできない。地図への遷移だけはつながらないが、
  地図は外部アプリに投げる設計なので直さない。
  通知を不採用にしたので、**このアプリは権限を1つも要求しない**ことも確定した。
  **フェーズCで残るのは C-7(実際に1回旅行して使う)だけ。**
- 2026-08-10 **C-4 Share Extension を実装した**(保留を解除)。
  共有シートの「たびのしおり」→ App Group → アプリが起動・復帰時に拾って
  **インボックス**に溜め、旅の中で Day に配置する(docs/ux-design.md §4.4)。
  Xcode を持たないので `project.pbxproj` を手で編集したが、
  **`xcode` パッケージでパースして検証済み**(2ターゲット・未定義参照なし・
  埋め込みフェーズ dstSubfolderSpec=13・Bundle ID と entitlements の対応)。
  ⚠️ **Apple 側の作業が終わるまでビルドは通らない**(App Group と拡張用 App ID)。
- 2026-08-10 **共有シートに出るところまで実機で通った**(pbxproj・拡張のコンパイル・
  埋め込み・2ターゲットの署名)。ただし**押しても何も届かなかった** ──
  `@capacitor/preferences` の `group` は App Group ではなく**キーの接頭辞**で、
  読むのは常に `UserDefaults.standard` だった(実装を読んで判明)。
  受け渡しを **App Group のファイル → SceneDelegate → Documents → Filesystem**
  に作り直し、preferences は依存から外した。
  あわせて**インボックスの入口を旅一覧へ移した** ── 旅の中に置くと、
  旅の数だけ同じ帯が出て「この旅に1件」と読めてしまうため(端末のものは端末の画面に)。
  拡張も無言をやめ、結果を一言出して閉じるようにした。
- 2026-08-10 **フェーズD の課金が実機で一周した。**
  購入・機内モードでの保持・復元まで確認。3つ踏んだので残しておく:
  - **Sandbox の価格がドルで出た。** サンドボックステスターを日本にしても
    直らず、ストアフロントが米国に落ちたまま抜けられなかった。
    **円で返ったときだけ StoreKit を信じ、円以外は日本の定価を出す**ことにした
    (日本のみ配信なので本番では常に StoreKit の値が出る)。
    価格を丸ごとハードコードすると価格改定で表示だけが古くなるので、そうしない
  - **`.tabishiori` を LINE が送れなかった。** 独自拡張子を諦めて `.json` へ。
    中身は元から JSON なので形式は変わっていない。見分けはファイル名の頭で付ける。
    **古い `.tabishiori` も引き続き開ける**(すでに人の手元にあるので)
  - **リード文の `**…**` がそのまま画面に出ていた。** 審査で最初に見られる画面なのに、
    TestFlight まで気づかなかった。文言を `{t(...)}` に素通しすると起きる
- 2026-08-10 **受け取りが再び壊れていた**(C-5 の再発)。
  「開いてもアプリが起動するだけ」で、原因は `Filesystem.readFile` に渡すパスの形。
  失敗を `catch {}` で握りつぶしていたので**無反応にしか見えなかった**のが本当の問題で、
  読めなければ画面に出すようにした。**黙る実装は、原因の特定まで丸ごと奪う。**
